{% extends 'base.html.twig' %}

{% block body %}
    <h1>Création d'un post</h1>

    {{ form_start(form, {'attr': {'class': 'col-lg-8'}}) }}
        {{ form_row(form.content, {'label': 'Commentaire'}) }}
        {{ form_row(form.note, {'attr': {'min': '0', 'max': '10'}}) }}

        <input type="submit" value="Envoyer le commentaire"  class="btn btn-lg" />
    {{ form_end(form) }}


    {#<a href="{{ path('serie_show', {'id': post.serie.id }) }}">Retour à la série</a>#}

{% endblock %}
